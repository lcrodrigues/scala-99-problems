object Ex04 {
    def main(args: Array[String]) = {
        val list = List(1, 2, 3, 4, 5)
        println(length(list))
    }

    def length(list: List[Any]): Any = {
        return list.length
    }
}
